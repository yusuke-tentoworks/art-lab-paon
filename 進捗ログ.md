# 進捗ログ: ART LAB PAON Webサイト

運用ルール:
- 新しい日付を**上に**追記する（最新が一番上）。
- セッション終了時に必ず更新する。次回セッションはこのログを読んでから着手する。
- 機密情報（パスワード・APIキーの値）は書かない。

## 期日

このプロジェクトの締切・開催日・公開予定日を書く。無ければ「なし」のままにする（空欄にしない）。
日付は `YYYY-MM-DD` の絶対日付で書く。期日が変わったらこの欄を書き換え、経緯は下のログに追記する。

なし

---

## 2026-08-19（OGP画像を 1.91:1 に作り直した／左右の余白は無し）

### 決定事項
- **OGP画像は「上下が切れてもよい。左右の余白は無し」の方針で確定**（人の判断）。
  8/18に用意した案B（余白を足して全体を収める）は採らない。
  元の縦横比のまま各SNSに切らせるのではなく、**こちらで 1.91:1 に切った画像を持つ**ことにした。
  こうすると X も LINE も Facebook も同じ絵が出る。

### 完了したこと
- **1.91:1 のOGP画像を2枚生成**（Pillow・quality=85）
  - `hero-visual-mono-ogp-wide.jpg` … 1200×630・221KB。
    元は高解像度の `hero-visual-mono.webp`（2480×2119）から切って縮小した。
    切り位置は**中央より少し下**（1200幅換算で上から250px）。
    天井を落として、手前の机2列と正面の壁画が全部入るようにした。
  - `concept-art-color-ogp-wide.jpg` … 1024×538・180KB。
    元が1024幅なので**拡大はしていない**（横1024のまま）。
    切り位置は**下端合わせ**にして、**左下の署名を残した**。
    そのぶん鼻の先と頭の上が切れる。署名（作品の一部）を残す方を優先した。
- **5ページの `og:image` / `twitter:image` を新ファイルへ差し替え**、
  `og:image:height` も実寸に修正（1025→630、ギャラリーは 726→538）。
  index / gallery / privacy / thanks / 404 の5ファイル。
- **旧OGP画像2枚を削除**（`hero-visual-mono-ogp.jpg` / `concept-art-color-ogp.jpg`）。
  参照は残っていないことを確認済み。ファイル名を変えたので、
  Xのカードキャッシュ（URL単位・約7日）を待たずに新しい画像が読まれる。
- ローカルサーバー（`python3 -m http.server`）で5ページと画像2枚が 200 で返ることを確認。

### 完了したこと（追記・push済み）
- **commit・push 完了**（`dfaf0d5`）。Netlifyの本番で以下を確認した。
  - トップ / ギャラリーの `og:image`・`twitter:image` が新ファイルを指している
  - 新画像2枚が 200・`image/jpeg` で返る
  - 旧画像2枚は 404（削除が本番に反映されている）

### 確認済み（2026-08-19）
- **ギャラリーの切り位置（下端合わせ・左下の署名を残し、鼻の先と頭の上が切れる）は
  中川様に確認済み。このままでよい。** OGP画像まわりで中川様待ちの案件は無くなった。

### 未完了・次回やること
- X / LINE で実際にカード画像が出るか確認する（ファイル名を変えたのでキャッシュ待ちは不要。
  8/22以降に確認、という8/18の先送りはもう不要）。

## 2026-08-18（下層3ページへのOGP追加／専用OGP画像の確認資料）

### 完了したこと
- **`privacy.html` / `thanks.html` / `404.html` に OGPタグを追加**（2026-08-15 に積み残していた分）。
  index.html のブロックを踏襲し、`og:title` / `og:description` / `og:type` / `og:url` /
  `og:image`（+ type・width・height・alt）/ `og:site_name` / `og:locale` と
  `twitter:card` / `twitter:image` を設置。画像は3ページとも
  `hero-visual-mono-ogp.jpg`（1200×1025）を流用した。
  404 と thanks は `noindex` のままで、OGPを足しても検索には出ない。
- `gallery.html` に `og:locale` が抜けていたので追加（他ページと揃えた）。
- **1200×630 の専用OGP画像の要否を判断するための確認資料を作成**（中川様への提示用）。
  Xが実際に切り出す 1.91:1 の範囲を元画像に重ねた図と、
  案A（現状のまま＝切られた結果）／案B（余白を足して全体を収めた 1200×630）の見本を生成した。
  - **トップの内観写真は案Aで問題なし。** 切れるのは天井と手前の床だけで、教室の様子は残る
  - **ギャラリーの作品「ゾウ」は案Aだと左下の署名と頭の上部が切れる。** ここだけ判断が必要
  - 案Bの画像は生成済み（確認待ちのためリポジトリには入れていない）
  - 資料URL: https://claude.ai/code/artifact/89ad85dc-771e-4a3b-bbd6-24a221197cba （非公開）

### 未完了・次回やること
- **上記4ファイルの変更はローカルのみ。commit・push していない**（mainへのpushは即本番反映のため）。
- 2026-08-22以降にXでカード画像が出るか確認する。
- 1200×630の専用OGP画像を作るか中川様に確認する（確認資料は上記のとおり作成済み）。

## 2026-08-18（OGP確認を8/22以降に先送り）

### 決定事項・申し送り
- カードキャッシュのTTL（約7日）を踏まえ、Xでのカード画像表示確認は
  **2026-08-22以降**に行う方針とした（人の判断）。それまでは確認不要。
  1200×630の専用OGP画像を作るかの中川様への確認は、このスケジュールとは別に対応する。

### 未完了・次回やること
- 2026-08-22以降にXでカード画像が出るか確認する。
- 1200×630の専用OGP画像を作るか中川様に確認する。

## 2026-08-15（X でOGP画像が表示されない問題の修正）

### 背景
インクルーシブ豊川文化祭の調査で、**X（Twitter）は `og:image` が WebP だとカード画像を
表示しない**ことが判明した。当サイトも `og:image` が WebP で、井上さんの確認でも
X で画像が出ないことを確認済み。

判定の根拠（3サイト比較）:

| サイト | og:image | twitter:image | X表示 |
|---|---|---|---|
| てんとコーヒー | PNG | PNG | ○ |
| **ART LAB PAON** | **WebP** | なし | **✕** |
| インクルーシブ豊川（当時） | WebP | JPG | ✕ |

X は仕様と異なり `twitter:image` を優先せず `og:image` を読むため、
**直すべきは `og:image` の方**。LINE・Threads・Facebook は WebP を表示できるので
X だけで発覚する。

### 完了したこと
- **OGP用のJPGを2枚生成**（元WebPからPillowで変換・quality=80）
  - `hero-visual-mono-ogp.jpg` … 1200×1025・278KB（元は2480×2119なので長辺1200へ縮小）
  - `concept-art-color-ogp.jpg` … 1024×726・202KB（元サイズのまま。拡大はしない）
  - **意図的に元の縦横比を維持した。** 1200×630（2:1）に切ると
    ゾウの絵の署名が切れるなど、作品の見え方を私の判断で変えることになるため
- **`og:image` をJPGへ差し替え**（index.html / gallery.html）。
  あわせて `og:image:type` / `width` / `height` / `alt` を追加
- **`twitter:image` を新規追加**（これまで未設定で、X は og:image だけを見ていた）
- サイト本体の画像は WebP のまま。変更したのは OGP に使う1枚ずつだけで、
  ページ表示速度への影響はない（クローラーが取りに来る時だけ取得される）

### 未完了・次回やること
- **X でカード画像が出るか確認する**（投稿欄にURLを貼ればプレビューで判定できる）。
  push・Netlifyのデプロイは 2026-08-15 に完了し、JPG2枚が本番で HTTP 200 を返すこと、
  配信HTMLが新しいタグを持つことは確認済み。あとは X での実表示のみ。
  出ない場合はカードキャッシュ（TTL約7日）の可能性があるので1週間は様子を見る。
  X の Card Validator は2022年に廃止済みで手動再クロールはできない
- **1200×630 の専用OGP画像を作るかを中川様に確認する。** 現状は作品画像をそのまま使うため、
  X のカード表示では上下が切られる。作品の切れ方を許容するか、
  余白付きの専用画像を作るかは、作品の見せ方の判断なので勝手に決めない
- **`privacy.html` / `thanks.html` / `404.html` に OGPタグが無い**（2026-08-15 の全サイト監査で判明）。
  優先度は低いが、`thanks.html`（送信完了ページ）は稀に共有されうる。
  手を付けるなら index.html の OGPブロックをコピーし、タイトルと説明文を各ページに合わせる

## 2026-08-04

### 完了したこと
- 使われていない古いブランチを削除し main へ一本化
  - `production`（最終コミット 2026-05-07 で停止）と `SEO修正` を、リモート・ローカルとも削除
  - 両ブランチのコミットはすべて main に含まれており、失われたものはない
  - Netlify のデプロイ元は 2026-07-24 に main へ変更済みのため影響なし
- 追跡されていた `.DS_Store` 13件を `git rm --cached` でGit管理から除外
  - `.gitignore` には以前から記載があったが、記載前にコミット済みのファイルは追跡が継続していた
  - ローカルのファイル自体は削除していない

### 未完了・次回やること
- なし

### 決定事項・申し送り
- 本番は **main への push で Netlify が自動ビルド・公開**。production ブランチは廃止した。
- `assets/images/gallery_backup/` が公開ディレクトリ配下に残っている。配信対象に含まれるため、
  不要であれば削除を検討する（今回は判断を保留）。

---

## 2026-07-24（3回目）

### 完了したこと
- 講師（中川一也）のオンラインショップ（BASE: https://nakakazu.base.shop/ ）を紹介するSHOPセクションをindex.htmlに新設
  - 配置: INSTRUCTORセクションの直後、ACCESSの前（ギャラリー＝生徒作品と混同しないよう講師紹介の延長に配置）
  - 画像なし・テキスト+CTAボタンのみの構成（`assets/css/style.css` に `.shop` `.shop__content` `.shop__desc` を追加）
  - ヘッダーnav（Instructor/Access間の並びの末尾）とフッター（プライバシーポリシーの並び）にも外部リンクを追加
  - ヘッダー/フッターは `assets/js/components.js` 内のJS埋め込みHTML（COMPONENTSオブジェクト）が実際に描画に使われている実装だったため、そちらを修正（`assets/components/header.html` `footer.html` は未使用の複製ファイルだが整合性のため同様に修正）
  - ローカルサーバーで表示確認済み（DOM構造・リンク先URL・スタイル適用を確認。ブラウザ側のスクロール描画ツールが不安定だったためコンピューテッドスタイル経由で検証）

### 未完了・次回やること
- 上記変更は未コミット・未push（Netlify本番未反映）
- ショップの紹介文言は暫定（「教室でご紹介しきれない作品も多数掲載しております。」）。中川様確認後、必要なら調整

### 決定事項・申し送り
- オンラインショップは既存の外部サービス（BASE）へのリンクのみで、サイト内に決済・EC機能は持たない方針

---

## 2026-07-24（2回目）

### 完了したこと
- gallery.html に未掲載だったGoogle Drive上の新規作品画像を追加（計21枚）
  - Drive フォルダ4つ（Adult-Acrylic / Artistic-Painting / Digital-Illustration / iPad、各コース名と一致）を既存ローカルファイルと突き合わせ、未追加分のみ抽出
  - 内訳: Adult-Acrylic-Painting-Course +1（3枚目）、Artistic-Painting-Course +6（14〜19枚目）、Digital-Illustration-Course +14（12〜25枚目）、iPad-Course は新規なし
  - 画像は既存運用に合わせ長辺1600pxにリサイズ（EXIF Orientation補正込み）→ jpeg/png保存 → cwebp -q80 でwebp生成
  - gallery.html にitem追加、各セクションのitem数コメントも実数に合わせて修正（造形絵画は元々12枚が実数で「13 items」表記が誤りだったため18に修正）
  - `assets/js/main.js` の `GALLERY_PREVIEW_IMAGES`（トップページのランダムプレビュー用の番号リスト）も新規番号を追加
  - ローカルサーバーで gallery.html（全カテゴリフィルタ）とトップページのプレビューをブラウザ確認、53枚全画像のロード成功を確認済み

### 未完了・次回やること
- 上記変更は未コミット・未push（Netlify本番未反映）
- 新規画像のalt文言はコース内の連番のみで内容の説明文言までは付与していない（既存パターンを踏襲）

### 決定事項・申し送り
- Driveの新規ファイルは日付や無題番号のファイル名だったため、ローカル命名規則（コース名+連番.拡張子）にリネームして保存

---

## 2026-07-24

### 完了したこと
- トップページ（index.html）に「GALLERY 作品ギャラリー」セクションを新設
  - Concept と Course の間に、4コース分のサムネイル（2×2グリッド）＋「VIEW ART GALLERY」ボタンを配置
  - Course セクション最下部に埋もれていた旧ギャラリーバナー（インラインスタイル）は削除
  - CSSは `.gallery-preview__*` として `assets/css/style.css` に追加
- ギャラリープレビューのサムネイルをアクセスごとにランダム表示に変更
  - 各コースの画像フォルダ内の実在ファイル番号を `assets/js/main.js` の `GALLERY_PREVIEW_IMAGES` に定義し、ページ読み込み時にランダムで1枚を選択
  - 画像を追加・削除した場合は `GALLERY_PREVIEW_IMAGES` の番号リストも合わせて更新が必要

### 未完了・次回やること
- gallery.html の内容更新（本セッションで対応済み。詳細は上の「2026-07-24（2回目）」参照）
- 上記コミットは未push（コミットのみ実施済み、Netlify本番未反映）

### 決定事項・申し送り
- ギャラリーへのアクセス改善は「新規プレビューセクション追加」ではなく「既存バナーの位置・目立ちを強化」の方針で対応（ユーザー選択）

---

## 2026-07-13

### 完了したこと
- 旧Antigravityルールから新ルールブック（tentoworks-dev-rules）準拠の運用へ移行
  - CLAUDE.md 新設、ガイドラインのコピー・.aiignore を削除（参照方式へ）
  - .claude/settings.json（permissions.deny）配置、.gitignore 追記

### 未完了・次回やること
- なし（移行完了。通常の保守運用へ）

### 決定事項・申し送り
- デプロイ方式: mainブランチへのpushでNetlifyが自動ビルド・公開（ステージング: GitHub Pages）
